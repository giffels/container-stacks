- name: Add addtional scripts
  include_tasks: add_scripts.yaml
  loop: "{{ ADDITIONAL_SCRIPTS | dict2items  }}"
  loop_control:
    label: "Adding {{ item.key }} to {{ item.value.dest }}"
  when:
    - ADDITIONAL_SCRIPTS is defined
